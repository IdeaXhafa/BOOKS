@model BOOKS.Models.Pagesa;

@{
    ViewData["Title"] = "Create";
}

<h2 style="color:white">Create</h2>


<div class="modal-dialog">
    <div class="modal-content">
        <form class="form" asp-action="Create" method="post">
            <div class="modal-header">
                <h4 class="modal-title">Shto Pagesë</h4>
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true" onclick="window.location='/pagesa';">&times;</button>
            </div>
            <div class="modal-body">
                <div class="form-group">
                    <label>Shuma:</label>
                    <select asp-for="Shuma" class="form-control" asp-items="@(new SelectList(ViewBag.shuma))">
                    </select>
                </div>
                <div class="form-group">
                    <label>Klienti:</label>
                    <select asp-for="ClientId" class="form-control" asp-items="@(new SelectList(ViewBag.klientetid,"id","Emri"))">
                    </select>
                </div>
            </div>
            <div class="modal-footer">
                <input type="button" class="btn btn-default" value="Anulo" onclick="window.location='/pagesa';">
                <input type="submit" value="Create" class="btn btn-primary float-right">
            </div>
        </form>
    </div>
</div>

